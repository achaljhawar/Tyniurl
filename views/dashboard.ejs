<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <title>URL Database</title>
		<link rel="stylesheet" href="dashboard.css"/>
		<link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/css/bootstrap.min.css" integrity="sha384-Vkoo8x4CGsO3+Hhxv8T/Q5PaXtkKtu6ug5TOeNV6gBiFeWPGFN9MuhOf23Q9Ifjh" crossorigin="anonymous">
  </head>
  <body>
    <div style="text-align: right; margin: 20px;">
   	<button type="button" class="btn btn-success" onclick="window.location.replace('/')">Take me back</button>
  </div>
	<center>
		<table class="styled-table">
			<thead>
					<tr>
							<th class="switch" >
									Full URL
							</th>
							<th class="switch" >
									Short URL
							</th>
							<th class="switch" >
									Clicks
							</th>
					</tr>
			</thead>
				<tbody>
						<% shortUrls.forEach(shortUrl => { %>
								<tr class="switch">
									<td><a href="<%= shortUrl['fullurl'] %>"><%= shortUrl['fullurl'] %></a></td>
									<td><a href="<%= shortUrl['shortid'] %>"><%= shortUrl['shortid'] %></a></td>
									<td class="switch"><%= shortUrl['clicks'] %></td>
								</tr>
						<% }) %>
				</tbody>
			</table>
		</center>
  </body>
</html>